# 文档任务流水线精简

<!-- 核心问题: 需求到哪了、验收标准是什么？
     定位: 需求级进度跟踪
     不属于本文档: 产品全局规划（→ roadmap）、技术方案（→ tech）、系统架构（→ arch） -->

## 1. 问题

<!-- 为什么要做这个需求。回答"值不值得做"
  - 3 要素，每要素 1-2 句: 用户痛点（具体困难）、影响范围（多少人/多频繁）、为什么现在做（触发事件）
  - EXCLUDE: 技术实现原因、内部架构限制 -->

文档类任务（写 PRD、tech spec、更新文档）走完整 sprint 流水线时，plan 和 quality 阶段产出低价值——plan 拆分结果永远是"1 个文档 + 1 个索引更新"，quality 只检查"文件存在"。每次文档任务浪费 2 个阶段 + 2 轮确认。v1 实施过程中 4 个文档 sprint 均出现此问题。

## 2. 目标用户

<!-- 给谁做。回答"谁会用"
  - 用户角色 + 使用场景 + 当前替代方案及其不足
  - EXCLUDE: 系统内部组件名、数据模型 -->

使用 sprint 驱动文档编写的开发者。当前替代方案是手动快速确认（说 continue），但 plan 和 quality 阶段仍会执行和输出，增加不必要的交互轮次。

## 3. 核心假设

<!-- 做了之后会怎样。回答"在验证什么假设"
  - 假设 = 1 句因果句（"做了 X → 用户会 Y"），验证方式 = 可执行的检查
  - EXCLUDE: 多个假设混在一起、技术方案、实现细节 -->

**在 Input Normalization 中识别文档类任务并自动跳过 plan + quality → 文档任务减少 2 个阶段和 2 轮确认，且不丢失必要的质量保障。**

验证方式：文档类描述触发精简流水线（无 plan/quality）；代码类描述不受影响。

## 4. 方案

<!-- 做什么（不是怎么做）。回答"用户体验怎么变"
  - 用 before → after 格式描述用户行为变化，每个变化点 1 句
  - 任务拆分: 链接到每个 tech 文档（树状向下索引，不回指 roadmap）
  - EXCLUDE: 操作流程图、交互细节、内部数据结构、存储格式、算法、文件路径 -->

- **Before**: `/sprint 写 M4 PRD` → 6 个阶段（含低价值的 plan 和 quality） → **After**: 自动识别文档任务 → 3 个阶段（brainstorm → execute → insight）

### 任务

| 任务 | 文档 | 进度 |
|------|------|------|
| 流水线精简实现 | — | 1/1 |

## 5. 验收标准

<!-- 怎么算做完了。回答"验收标准"
  - 条目列表，每条格式 = "用户做 X → 应看到 Y"，覆盖核心场景+关键边界，每条可独立验证
  - EXCLUDE: 单元测试用例、代码覆盖率、内部接口断言 -->

- 描述包含文档关键词 → evaluate 输出"类型: 文档任务"，流水线不含 plan 和 quality
- 描述不含文档关键词 → 流水线与之前完全一致
- 代码类任务的回归测试通过

## 6. 排除项

<!-- 不做什么。回答"边界在哪"
  - 格式 = "不支持 X" 或 "X 推迟到 vN"，只列容易被误认为在 scope 内的项
  - EXCLUDE: 技术债、重构计划 -->

- 通用任务类型感知裁剪（只处理文档类）
- 文档模板/脚手架工具
- stage 文件内部逻辑变更
